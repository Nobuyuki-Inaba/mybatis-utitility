<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.SampleMapper">

    <select id="selectById" resultType="com.example.Sample">
        select *
        from sample
        where update_date = #{businessDate}
          and id = #{id}
    </select>

    <select id="selectByName" resultType="com.example.Sample">
        select *
        from sample
        where update_date = #{businessDate}
          and name like #{name}
    </select>

    <select id="selectList" resultType="com.example.Sample">
        select *
        from sample
        where 1 = 1
        <if test="businessDate != null and businessDate != ''">
          and update_date = #{businessDate}
        </if>
        <if test="status != null">
          and status = #{status}
        </if>
        order by id
    </select>

    <select id="selectWideTable" resultType="map">
        select * from wide_table
    </select>

    <insert id="insert">
        insert into sample (id, name, update_date)
        values (#{id}, #{name}, #{businessDate})
    </insert>

    <update id="update">
        update sample
        set name = #{name},
            update_date = #{businessDate}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from sample
        where id = #{id}
    </delete>

</mapper>
